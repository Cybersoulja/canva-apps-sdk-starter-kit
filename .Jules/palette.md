## 2024-05-24 - [External link icon]
**Learning:** Added a reusable pattern for external links in `@canva/app-ui-kit` to be `icon={OpenInNewIcon}` and `iconPosition="end"` to Buttons.
**Action:** Always add `OpenInNewIcon` to `Button` components that open external links.
