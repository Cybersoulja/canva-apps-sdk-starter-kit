## 2024-05-15 - Visualizing External Links
**Learning:** Adding an external link icon to buttons that open external URLs provides clear visual feedback to users about what action the button performs.
**Action:** Always add `icon={OpenInNewIcon}` and `iconPosition="end"` to Buttons that act as external links in the Canva UI kit.
