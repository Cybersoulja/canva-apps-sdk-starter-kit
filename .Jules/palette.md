## 2024-05-02 - Add OpenInNewIcon to external links
**Learning:** A reusable pattern for external links in this kit is adding `icon={OpenInNewIcon}` and `iconPosition="end"` to Buttons to give visual indication that they navigate away from the current context.
**Action:** Always append this icon to buttons that trigger `requestOpenExternalUrl` or similar external navigations.
