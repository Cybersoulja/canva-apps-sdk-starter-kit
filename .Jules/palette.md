## 2023-10-27 - External Link Indication
**Learning:** External links should visually indicate that they will open in a new tab/window to set user expectations properly.
**Action:** Use `OpenInNewIcon` with `iconPosition="end"` on `Button` components that trigger `openExternalUrl` from `@canva/platform`.
